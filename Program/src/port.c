
#include "packet.h"





/*
void ss_recvfrom(client *client) {


    #ifdef POSIX 
        //    recvfrom(sfd, buff, PACKET_LEN, 0, (struct sockaddr*)client, &client_len);
    #endif

}
*/



       //ssize_t recvfrom(int sockfd, void *buf, size_t len, int flags, struct sockaddr *src_addr, socklen_t *addrlen);
